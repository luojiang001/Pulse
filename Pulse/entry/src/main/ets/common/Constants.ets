export class Constants {
  static readonly DOMAIN: string = '192.168.128.100';
  static readonly BASE_URL: string = `http://${Constants.DOMAIN}:8080`;
  static readonly WS_URL: string = `ws://${Constants.DOMAIN}:8080`;
}
